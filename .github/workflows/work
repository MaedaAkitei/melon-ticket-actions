name: example
on:
  schedule:
    - cron: '*/1 * * * *' # Every 5 minutes
jobs:
  job:
    runs-on: ubuntu-latest
    timeout-minutes: 1
    steps:
      - name: Check Tickets
        uses: maeda
        with:
          product-id: 212765
          schedule-id: 100001
          seat-id: 1_0
          dingtalk-webhook-url: ${{ secrets.DINGTALK_WEBHOOK_URL }}  
          message: '티켓사세요'  
